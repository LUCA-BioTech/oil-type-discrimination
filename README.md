# Oil-Type Discrimination Using Enzyme Absorbance Signals

本项目利用 **类过氧化酶材料（15 种）** 与 **油类（13 种）** 的特异性反应所产生的 **吸光度特征** 来实现油类分类鉴别。

该项目使用以下工具实现了一个可复现的机器学习管道:

- StandardScaler
- LDA
- Gaussian Naive Bayes
- Stratified Cross-Validation

## 🔬 1. 实验背景

- 材料（酶）：15 种
- 油类：13 种
- 实验设计：
  **15 材料 × 13 油类 × 6 平行组 = 1170 个吸光度样本**
  （可扩展到 9 组平行，共 1755 条）

记录的变量：


| 变量      | 描述                      |
| --------- | ------------------------- |
| en1–en15 | 15 种酶材料对应的吸光度值 |
| cate      | 油类标签（13 种）         |

最终目标：给盲样输入（吸光值 + 材料），模型自动预测油类类别。

## 📁 2. 项目结构

```plaintext
oil-type-discrimination/
│── data/
│     ├── raw/              # 原始 CSV/数据
│     ├── processed/        # 标准化 / LDA 后输出
│── experiments/
│     ├── base_model/
│     │     ├── params.json # 超参数配置
│     │     ├── metrics.json# 评估指标输出
│     │     └── logs.txt    # 训练日志
│── model/
│     ├── ml_model.py       # 模型类/架构定义
│     └── utils.py          # 数据预处理 & 通用函数
│── train.py                # 主训练脚本
└── evaluate.py             # 评估/测试脚本

结构目的：
data/ 存放数据
model/ 存放模型定义及预处理函数
train.py 负责训练 + CV + 保存结果
evaluate.py 负责模型测试/绘图/生成报告
experiments/ 存放实验配置及结果总结

```

## 🧪 3. 安装依赖

Python 版本建议：**3.8–3.10**

```bash
pip install -r requirements.txt
```

## 📊 4. 数据准备

将原始实验数据放入：data/raw/data-923.csv

## 🏋️ 5. 模型训练

python src/train.py

## 📈 6. 性能评估

查看某个模型的详细分类报告：

```
python src/evaluate.py

output
Cross-validation metrics: experiments/base_model/metrics.json
Confusion matrix: experiments/base_model/confusion_matrix.png

```

## 🧪 7. Notebook（EDA）

项目包含：

notebooks/iol_classification.ipynb,对应文章的算法和可视化代码

notebooks/model.ipynb,增加模型泛化能力证明

内容包括：

- 模型训练
- 可视化
  - Feature Correlation Matrix → 展示高相关特征，说明 LDA 合理性
  - LDA Scree Plot → 前 3 维覆盖 80% 变异，说明降维选择
  - GaussianNB 2D/3D Prediction → 前 2 维坐标 + 模型预测标注，突出预测效果
  - ROC 曲线 → 模型判别能力量化

## 📘 8. 依赖

- scikit-learn
- pandas / numpy
- matplotlib / seaborn
